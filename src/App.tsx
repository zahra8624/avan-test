import { BrowserRouter } from "react-router-dom";
import { AppContainer } from "./AppContainer";

function App() {
  return (
    <BrowserRouter>
      <AppContainer />
    </BrowserRouter>
  );
}

export default App;
